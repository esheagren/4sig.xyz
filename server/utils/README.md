# Scoring

The server-authoritative scoring implementation is in `api/_lib/scoring.ts`. The local Express server and Vercel API both use it. See the root README for the formula and integration tests.
